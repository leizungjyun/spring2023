# 纵置二轮车研究方案 · 2026-09-17

中文企业交流演示，共 9 页。保留原有封面信息、团队照片、院系标识与原始媒体；原页面及局部样式的修改前副本在 `original-before-build/`。

## 预览与维护

在仓库根目录运行 `npm start`，打开 `/collections/external-presentations/2026-09-17-single-track/`。默认深色，网址追加 `?theme=white` 等可切换仓库主题。方向键导航，Escape 概览，S 打开演讲者备注。`slides.md` 使用 `===` 横向、`==` 纵向分隔；`Note:` 后为备注。

布局与交互仅由本目录 `presentation.css`、`presentation.js` 定义，框架仍引用仓库共享目录。视频静音、自动循环播放，切页后暂停；保留播放控件。原视频较大，静态托管首次加载时间受网络影响。

## 内容与材料

1. 封面：保留用户既有标题与单位信息，使用 teaser.png。
2. 多地形自平衡：道路、砾石、草地、坡道、台阶、负载六段实车视频。
3. 虚实迁移：平坦与台阶仿真视频；新增可编辑 `media/sim-to-real.svg`。
4. 跨载具：自行车、电摩道路/非铺装路面、E-bike 展示与两段载人视频。
5. CycleX 方法：两幅提供的原图，可点击查看完整图。
6. CycleX 结果：PDF 矩阵直接渲染成 PNG，点击可打开原 PDF。
7. 车道保持：L2.mp4；新增可编辑 `media/lane-control.svg`。
8. 团队与专利：保留照片和标识，核实四份材料为发明专利申请公开文本。
9. 结尾：科交会.mp4 原视频。

所有引文与实验解读在对应演讲备注。官方论文摘要页面留存于 `evidence/`；抽帧、媒体清单、专利文字和检查预览在 `qa/`。新增视频封面帧在 `media/posters/`，原视频及其他未采用素材未删除。

## 需补充或确认

- 多地形及跨载具视频对应的策略版本、载具配置、人工/远程介入记录；坡度、台阶、负载、速度与重复试验记录。
- CycleX 的评估环境、成功判据、样本量、置信区间与训练随机种子；矩阵目前仅支持所示四源策略的等权平均比较。
- CycleX 完整作者列表与投稿版本；ICRA 2027 投稿中来自本次提纲，不代表录用。
- 视觉车道检测的实际集成版本、标定与运行条件；“L2”沿用提纲标签，不作为标准分级认证。
- 专利当前法律状态、许可条件，以及拟合作企业的应用需求与验收标准。

提纲“无陀螺仪”修正为“无陀螺稳定器”：提供的硬件专利包含 IMU 陀螺仪测量，姿态传感器与产生平衡力矩的装置不能混同。

## 检查

`qa/validation.json` 记录 9 页在 black/white 下的图片加载、纵向溢出、视频静音/循环/播放状态、离页暂停和 Escape 概览检查。`qa/slide-*.png` 为浏览器截图。检查脚本为本地 QA，不参与演示加载。
