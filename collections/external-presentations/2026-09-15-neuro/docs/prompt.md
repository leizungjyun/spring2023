请为 collections/external-presentations/2026-09-15-neuro/ 制作中文演示文稿，向风险投资人介绍我们的类脑芯片方案。
先检查当前文件与已有材料，保留我的最新修改。新增文件仅放在该 deck 目录内。
- 不讨论融资计划。
- ===：新横向页；==：新纵向页。
- Part 名称仅用于组织，不另做章节标题页。
- 沿用现有深色风格、统一两栏布局。
- 以图为主，以短语为主。
- 每页一个核心结论，最多一条完整结论句。
- 正文尽量不超过 3–4 个要点。
- 解释、来源、实验条件放入 Note。
- 不照搬提纲中的说明文字到幻灯片。
- 区分：已实测 / 仿真 / 目标 / 计划。
- 不编造性能、客户、成本或合作关系。
- 缺失信息简写“待补充”。
- 架构图使用可编辑 SVG。
- 真实芯片照片、实验图必须有来源。
- 视频静音、自动播放、循环播放。
- 保留 Escape 概览及既有交互行为。
关键词：
存算耦合 · 硬件非线性 · 高速边缘计算
署名、团队、日期：
使用已有信息。
===
短语：
- 低功耗
- 低延迟
- 频繁数据搬运
image：
1. 冯诺伊曼架构 SVG。
存储 ↔ 计算，突出往返箭头。
2. 真实芯片显微图。
标出 SRAM/cache 等存储模块。
Note：
选取有公开模块标注的具体芯片。
面积比例必须有依据。
不泛称所有芯片“大部分面积都是存储”。
面积占比与搬运能耗分别论证。
==
核心结论：
让计算靠近存储，减少数据搬运。
短语：
- 突触存储
- 局部计算
- 硬件神经网络
image：
冯诺伊曼架构 / 存算耦合架构对比 SVG。
继承上一页：
相同配色、模块形状、输入输出位置。
保留：
控制、接口、必要的数据转换模块。
Note：
使用“借鉴大脑的存算耦合原则”。
不把人脑等同于纯模拟电路。
海马体的功能分工与局部存算耦合并不矛盾。
==
table：
挑战 | 适用条件 | 应对方式
非通用 | 固定边缘任务 | 专用实现
有限精度 | 任务允许的精度余量 | 低精度验证
器件差异 | 任务相关的容错空间 | 冗余 / 硬件感知训练
image：
已有 scaling-up 实验图(error-params.pdf)。
Note：
根据真实实验说明规模、误差与任务性能的关系。
不能把“规模更大、精度更高”直接解释为容错性更强。
容错不等于无需可靠性或一致性管理。
同时考虑面积、功耗与规模扩展的代价。
===
图中主链路：
线性 → 非线性 → 线性 → 非线性
突出：
我们的技术位置与实际贡献。
image：
KANalogue teaser。
必要时选取关键面板，避免论文整图过密。
正文：
仅保留有证据支持的技术收益。
指标旁注明：实测 / 仿真 / 目标。
Note：
NDR 作为实现机制。
node efficiency 的定义、基线与器件细节放备注。
不在主页面展开器件物理推导。
==
短语：
- PCB 原型
- 神经网络运行
- 小车闭环验证
image：
PCB、小车。
video：
已有小车演示，静音自动循环播放。
data：
最多展示三个已有实测指标。
Note：
系统演示与芯片级验证分开。
不从视频推断延迟、功耗或精度。
===
路线图：
PCB → 验证芯片 → SiP / 模块 → 系统验证
短语：
- 成熟工艺
- 模块集成
- 规模扩展
核心价值：
高速响应，同等任务条件下追求更低能耗。
image：
有来源的 SiP 图片。
标注“封装形式示意”。
Note：
40 nm 作为拟评估方案，需有现有材料依据。
器件容错用于降低一致性要求，而非取消一致性要求。
评估工艺兼容性、外围电路、互连与封装。
系统能耗包含必要的转换、控制、存储和接口。
不把设计目标写成实测结论。
===
短语：
- 目标设备
- 当前瓶颈
- 可测收益
image：
传感输入 → 焦点估计 / 控制计算 → 执行器 → 光学响应
突出：
我们负责的计算环节。
data：
已有指标，或明确标记待测。
Note：
根据已有讨论补充目标客户与设备类型。
区分计算延迟、控制响应与完整对焦时间。
确认瓶颈是否位于计算环节。
不使用“做透就是上市公司”等无依据判断。
==
场景短语：
- 机器人触觉
- 柔性机械臂
- 高速工业伺服
image：
统一风格的三个场景图。
每个场景最多一条需求短语，且必须有依据。
Note：
区分采样率、控制更新率、闭环带宽和端到端延迟。
不以“200 Hz 以上”单独证明方案优势。
明确哪些技术可复用，哪些需要重新验证。
==
短语：
- 视觉推理
- 自动驾驶感知
- 更大规模网络
image：
计算规模扩展示意。
避免堆叠应用图片。
Note：
标注为长期方向。
说明与当前技术的复用关系及尚待验证的能力。
不暗示已完成产品或已有客户。
==
仅列场景名称：
- 制导炮弹
- 高超音速武器
呈现方式：
简洁的行业场景卡片。
Note：
仅作高层次场景列举。
不展开制导、控制、武器设计或性能提升方案。
不宣称已有产品适配、客户合作或应用验证。
===
短语：
- 核心成员
- 研究成果
- 工程原型
- 已有知识产权
image：
已有团队或成果素材。
Note：
只使用可核实信息。
区分论文、专利申请与专利授权。
不添加融资金额、资金用途或融资里程碑。
- 正文是否以短语为主？
- 每页是否只有一个核心结论？
- 能否用图替代解释文字？
- 实测、仿真、目标是否明确？
- 图片与指标是否有来源？
- 视频、Escape、版面是否正常？
最后单独列出待补充材料，不增加到演示正文。