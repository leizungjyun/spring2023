Title: Neuro Group Opening Keynote
Subtitle: Toward low-latency, low-power intelligence
Speaker: LI Shaun
Date: 2026-09-23
Theme: White
Language: English; Chinese retained for policy text and the closing quotation.

How to use this file: one section per slide, with the key point and the prompt behind each figure. Prompts marked **draw** are generated by `scripts/build-diagrams.py`; prompts marked **source** mean the asset is used as supplied and must not be re-plotted or redrawn.

===
**Slide 1 · Cover** — no figure.

===
**Slide 2 · AI is the main arena of U.S.–China competition**
Key point: AI is the main arena; the gap is in compute.

- **Figure A (left) — draw:** the three pillars of AI as a vertical linked chain — Algorithms, Data, Compute — with one status line under each ("Frontier model performance is converging", "Access and quality depend on the domain", "Hardware capacity remains uneven"). Highlight Compute on a tinted panel. Conceptual; no scores.
- **Figure B (right) — draw:** horizontal bars for the share of tracked AI-supercomputer performance: United States ~75%, China ~15%, other ~10%. Put each national flag at the end of its bar as a label, but do **not** fill the bar with the flag's own field colour — use a muted blue, a muted brick red and a neutral grey. Show the percentage outside each bar, with a 0–100% axis. Values from Pilz et al. (2025).

===
**Slide 3 · AI supercomputers are scaling faster than Moore's law**
Key point: post-Moore — supply scales fast, demand scales faster.

- **Figure A (left) — source:** Gordon Moore portrait (Computer History Museum), shown grayscale; beside it, Moore's original 1965 claim as a large "2× per year" with the qualifying sentence.
- **Figure B (right) — source:** the supplied `public-private-supercomputers.pdf`, rendered to PNG and linked to the full-size PDF. Reuse as supplied; do not re-enter its values as data, but do not re-plot it either.

===
**Slide 4 · Model-training demand is rising faster still**
Key point: training compute rises more steeply than supply.

- **Figure — build:** interactive log-scale scatter of ~15 historical models from `media/data/training-compute.json`, points labelled, hover and a selector showing date, PFLOP-days and a link to the model paper. Overlay three era trend lines transcribed from the supplied `computing-demands.pdf`: Moore's law (doubling every two years), the deep-learning era (doubling every 3.4 months) and the frontier era. Annotations, not fits.

===
**Slide 5 · Throughput is only half the story**
Key point: throughput is one objective; applications also need low latency.

- **Figure A (left) — source:** photograph of xAI's Colossus data centre, with a metric line "200 000 NVIDIA H100/H200 GPUs · March 2025". Clicking opens the original.
- **Figure B (right) — source:** a paired frame of two always-on perception settings — a consumer camera tracking focus, and a speed-enforcement camera on a highway — under the metric "Demand · Continuous, low-latency autofocus". Neither is a measurement; click to open the Commons source.

===
**Slide 6 · Intelligence must fit a resource budget**
Key point: energy is priced at every scale, from peak-hour service to the edge.

- **Figure A (left) — build:** pricing table for V4.1 Flash in CNY per million tokens, one column per billing tier with the peak tier first: 梁文峰 under "09:00–12:00 / 14:00–18:00" printed beneath the name, then 梁文谷 under "all other hours". Rows: input cache hit 0.04/0.02, input cache miss 2/1, output 8/4 — peak value first in each row. Tint the peak column amber, use tabular numerals, and add a note line "Same model, same tokens — 2× at peak hours."
- **Figure B (right) — draw:** three edge settings named by the constraint that binds each, not by product category: **Always-on audio** (a microphone icon) "Listening all day / on microwatts"; **Perception loop** (a camera icon) "Answers within / one frame time"; **On-body sensing** (a wrist sensor icon) "Months of runtime / on a coin cell". One card each, panel-filled, icon above a title, a hairline divider, then the two-line constraint. Conceptual design targets; no product claim.

===
**Slide 7 · Moving data costs energy and time**
Key point: the von Neumann bottleneck.

- **Portrait (left, above the figure) — source:** the Los Alamos identification photograph of John von Neumann, from Wikimedia Commons, where it is credited to LANL under an attribution licence; shown beside a one-line caption and linked to its Commons page.
- **Figure A (left) — draw:** the von Neumann organization in the deck's block language — a Memory block above a Processor block, labels only with no sub-caption under either, joined by a narrow amber path in both directions labelled "Move data", with an Input chip feeding the processor row on the left and an Output chip taking data out on the right. This is the master drawing that slide 8 reuses.
- **Figure B (right) — source:** the reference deck's GB202 die shot with English metadata and the white palette, showing the die surrounded by external GDDR7. Die photograph unchanged; layout is schematic.
- **Takeaway — build:** the slide's closing question, "Is the general-purpose von Neumann architecture the best fit for AI-specific applications?"

===
**Slide 8 · Learn from the brain's organization**
Key point: local memory, parallel dynamics, less movement.

- **Figure A (top) — draw:** the synaptic-state diagram translated into English and laid out as a wide band: three presynaptic axons converging on one shared dendrite, with synapse presence and strength stored at the contact point and the summed signal travelling to the soma. A legend on the right separates Memory (synapse presence and strength) from Compute (synaptic integration).
- **Figure B (bottom) — draw:** two panels in the same block language as slide 7. Left: separate Memory and Processor blocks with the amber "Move data" path, plus the input/output chips, titled "Separate memory and processing". Right: a "Compute-in-memory array" panel — an amber box holding a grid of weight cells with the caption "Weights stored in place" — titled "In-memory computing". Peripheral circuits omitted on both sides. A hairline rule between Figure A and Figure B — not one figure, and no summary line under them.

===
**Slide 9 · Analog imperfections can be a design constraint**
Key point: analog is poor at exact arithmetic, not at the accuracy a task needs.

- **Figure A (left) — redraw from measured data:** redraw the supplied `accuracy-against-errors.pdf` in English — accuracy (%) against weight error (%), one line per parameter count, markers at every measured point. Take the marker positions off the supplied figure rather than inventing a curve. Plot the smallest, second smallest and largest of its eight parameter counts, label each line at its right end with the count, and annotate the two extremes ("−1 point over the full range", "−8 points"). The message is both that accuracy survives large weight error and that capacity buys tolerance. The supplied figure stays linked; the credit line must say the chart is redrawn and that three of eight counts are shown.
- **Figure B (right) — build:** two-column objection table. Header row "Long believed" / "Why a fixed task can live with it"; rows: Imprecise, Noisy and drifting, Narrow, each with the reason a fixed inference workload can live with it. Source line: Rasch et al. (2023).

===
**Slide 10 · ACIM replaces adder trees with physical summation**
Diagram only, with short condition labels: compare the same local dot product using a DCIM adder tree versus ACIM current/charge summation and ADC readout. Highlight potential core energy and area savings at moderate precision; retain input encoding, converter cost and noise as compact labels. Sources are linked on the slide and detailed in the evidence register. No universal system-performance claim.

===
**Slide 11 · Neuromorphic computing: from national strategy to local industry**
Key point: policy runs from national strategy down to local industry.

- **Figure — build:** four policy cards, national to district, each with a Chinese title, date, issuer and one line on what it commits to: 国家 2017 (类脑理论与芯片), 广东省 2026 (计算架构与神经形态芯片), 深圳市 2022 (未来产业重点布局), 光明区 2022–2025 · 历史政策 (科研与成果转化支持). Titles link to the official documents; keep the Chinese, do not translate the cards.
- **Closing quotation — draw:** the quotation 神经形态计算是我目前最关心的方向 inside a plain white oval — one smooth closed line, 2.5px blue stroke, no fill shading, no scallops — with a short straight-sided tail on its left, angled up so it points at the head in the portrait. Keep it as simple as a stock speech-bubble icon. No attribution printed on the slide: the speaker names the source aloud, and `docs/evidence/sources.md` records it.

===

**Slide 12 · Current state of the art: computing inside the array**
Key point: both array-based routes reuse area the chip already has, and both compute coarsely.

- **Figure A (left) — draw:** an SRAM array of coarse 6T cells with two bitlines highlighted in amber, running down to a box reading "compute on the array's own bitlines". Below it: reuses the chip area and the mature logic process, but the cell is large and the array is not dense.
- **Figure B (right) — draw:** a denser DRAM array of 1T1C cells with two rows highlighted in amber and an elbow leading to a box reading "activate several rows at once". Below it: dense cells on a specialised process, with coarser arithmetic and a destructive read.
- **Takeaway — build:** the brief's advantage/disadvantage pair compressed to one line — area already paid for, against coarse arithmetic and a large cell. The brief's "low density" is carried on the SRAM side, which is where it belongs; the notes say so.

===

**Slide 13 · A memristor array computes with two circuit laws**
Key point: the weight is a conductance, so the multiplication happens at the crossing and the addition on the wire.

- **Figure (left) — draw:** a small crossbar with the conductance symbol at every crossing, rows labelled V₁–V₃ under an "Inputs" marker, each column ending in a summation node with an output I₁–I₃, and one crossing ringed in amber. Two heading lines inside the figure: Ohm's law at every crossing, Kirchhoff's current law at every column. Three by three for legibility; not a real array size.
- **Text (right) — build:** one paragraph per law, plus one line saying the weights never move. No measured values.

===

**Slide 14 · A predicted element, found thirty-seven years later**
Key point: a fourth circuit element was predicted from theory in 1971 and reported in a device in 2008.

- **Figure (left) — source:** the four two-terminal circuit elements as a taxonomy, from Wikimedia Commons — Parcly Taxel, CC BY-SA 3.0, vectorised from a diagram by Linear77. Reused unmodified; the credit line names the licence.
- **Papers (right) — build:** two cards, one per paper, each with the year in amber, the title linked, a one-sentence summary and the venue. 1971: Chua, "Memristor — the missing circuit element", IEEE Transactions on Circuit Theory 18(5), 507–519. 2008: Strukov, Snider, Stewart and Williams, "The missing memristor found", Nature 453, 80–83. The notes carry the contested-identification qualification; it does not go on the slide.
- **Takeaway — build:** the device remembers — its resistance depends on the charge that has passed through it.

===

**Slide 15 · Do we really need the memristor?**
Key point: the brief's analogy — a memristor array is to analogue computing what an FPGA is to digital logic.

- **Figure A (left) — draw:** an FPGA fabric — lookup-table blocks on a routing grid, with the routing switches and each block's configuration bit picked out in amber, and a legend line naming the amber parts as what the user configures.
- **Figure B (right) — draw:** the same fabric redrawn as a crossbar, every cell's conductance in amber, with a legend line naming the amber cells as the programmable weight. Drawn at the same scale and in the same block language as Figure A so the parallel is visible rather than asserted. FPMA is the speaker's coinage for this slide and the notes say so.
- **Takeaway — build:** the brief's analogy in the deck's own words.

===

**Slide 16 · Training and inference separate in practice**
Key point: training writes the weights inside a loop; inference reads fixed weights once.

- **Figure — draw:** a wide two-sided diagram. Left, "Training · data centre": data batches → forward and backward → update weights, with a dashed amber feedback path closing the loop, and a line below reading "every step changes the weights — many passes, data-centre power". Right, "Inference · device": sensor input → fixed weights → answer, with "the weights are read, never written — one pass, device power". A dashed divider between the halves, crossed once by an amber arrow labelled "weights".
- **Text (below) — build:** one line per side, keyed to the figure.
- **Takeaway — build:** the weights cross the gap once; the device that runs them only ever reads. The slide says "in practice" rather than the brief's 必然, and the notes say why.

===

**Slide 17 · Start from the computation, not the memory**
Key point: the brief's contrast — from memory, the storage decides what can be computed; from compute, the physics performs the operation.

- **Lead paragraph — build:** two sentences: at the edge, inference reads its weights and never rewrites them; most analogue in-memory computing still starts from a memory array and asks how to add computation to it.
- **Figure — draw:** two rows in the deck's block language, with a hairline between them. "From memory": a memory array → an amber arrow labelled "add compute" → an array that also computes, with the note "the storage decides what can be computed". "From compute": the operation to perform → "choose physics" → a device whose physics performs it, with the note "the array follows the function — what stores the weights comes after".
- **Takeaway — build:** from memory, the storage decides the computation; from compute, the physics does.

===

**Slide 18 · Nonlinearity from the device itself**
Key point: the brief's 利用器件物理特性实现可控非线性 — the device's own transfer curve is the nonlinearity.

- **Figure A (left, top) — source:** the reference deck's `nonlinearity.svg`, translated into English and re-paletted — linear transform → nonlinear transform → next layer, with "Technology acts here: device nonlinearity". Geometry unchanged.
- **Figure B (left, below) — draw:** this deck's own schematic of the function-basis idea: one input fanning out to three device transfer curves φ₁–φ₃, each weighted, summed at a node and read out as Σwᵢφᵢ(x). Generic — it is not a reproduction of the framework figure.
- **Figure C (right) — source:** the reference deck's `quantum-tunneling.svg` and `tunnel-diode-iv.svg`, translated, with the reference deck's Esaki portrait above them (Japan Academy, CC BY 4.0).
- **Credit — build:** the framework is KANalogue — Device-Native Kolmogorov–Arnold Networks for Analogue In-Memory Computing, with the copied framework PDF linked. The notes carry the reference deck's caveats on converters and on node efficiency.

===

**Slide 19 · Where we would start**
Key point: three application settings, every one of them a concept figure.

- **Figures — source:** the reference deck's `application-autofocus.svg`, `application-hypersonic.svg` and `application-touch.svg`, translated into English and re-paletted. Three numbered panels with the reference deck's three headings, each with a one-line qualifier: the autofocus fit, the "categories only" warning on the guidance panel, and the local-response constraint on the touch and arm panel.
- **Takeaway — build:** three settings where the loop is short and the weights do not change.

===

**Slide 20 · The argument in one slide**
Key point: rising AI demand and edge constraints lead through the data-movement bottleneck to compute-first, device-physical inference for short local loops. Four concise stages; no performance or product claim.

===

**Slide 21 · Closing image**
No figure. The last slide of the reference gathering deck: the group's own ending image, full-bleed, with the controls, progress bar and slide number hidden while it is on screen.
